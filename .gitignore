# Local OpenAI API key (never commit)
.openai-key

# macOS metadata
.DS_Store

# Transcriptions audio (utilisées localement pour calibrer la dictée)
*.srt

# Virtualenv Python local au skill (créé à la volée par les scripts)
.venv/
